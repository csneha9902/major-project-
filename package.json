{
  "name": "snn-ai-cognitive-health-optimizer",
  "private": true,
  "scripts": {
    "dev": "npx --yes kill-port 5173 5174 && npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  }
}
